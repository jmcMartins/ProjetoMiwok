<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.miwok">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NumerosActivity"
            android:label="@string/categoria_numeros"/>
        <activity android:name=".FamiliaActivity"
            android:label="@string/categoria_familia"/>
        <activity android:name=".CoresActivity"
            android:label="@string/categoria_cores"/>
        <activity android:name=".FrasesActivity"
            android:label="@string/categoria_frases"/>
    </application>

</manifest>